# Archived WordPress theme prototype

`wp-atmo-child/kadence-atmo` is **not** the canonical ATMO.BY WordPress child theme.

It is an early archived prototype from the initial docs/prototype repository import. Do not deploy it, copy from it, or use it as the source of truth for WordPress work.

Canonical child theme repository:

```text
https://github.com/r4t51/atmo-kadence-child.git
```

Canonical local child theme path:

```text
D:\Local Sites\atmo_redesign\app\public\wp-content\themes\kadence-child
```

Keep this archive only for historical comparison until explicit cleanup sign-off.
